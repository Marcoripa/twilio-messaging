import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marco.twiliomessaging',
  appName: 'twilio-messaging',
  webDir: 'dist/twilio-messaging/browser'
};

export default config;
